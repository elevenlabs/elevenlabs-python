# This file was auto-generated by Fern from our API Definition.

import typing
import urllib.parse
from contextlib import asynccontextmanager, contextmanager

import websockets.sync.client as websockets_sync_client
from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.jsonable_encoder import jsonable_encoder
from ..core.query_encoder import encode_query
from ..core.remove_none_from_dict import remove_none_from_dict
from ..core.request_options import RequestOptions
from ..core.websocket_compat import InvalidWebSocketStatus, get_status_code
from ..types.agent_call_limits import AgentCallLimits
from ..types.agent_sort_by import AgentSortBy
from ..types.asr_conversational_config import AsrConversationalConfig
from ..types.base_turn_config import BaseTurnConfig
from ..types.conversation_config_input import ConversationConfigInput
from ..types.list_speech_engines_response import ListSpeechEnginesResponse
from ..types.privacy_config_input import PrivacyConfigInput
from ..types.sort_direction import SortDirection
from ..types.speech_engine_config import SpeechEngineConfig
from ..types.speech_engine_conversation_initiation_client_data_config import (
    SpeechEngineConversationInitiationClientDataConfig,
)
from ..types.speech_engine_response import SpeechEngineResponse
from ..types.tts_conversational_config_input import TtsConversationalConfigInput
from ..types.vad_config import VadConfig
from .raw_client import AsyncRawSpeechEngineClient, RawSpeechEngineClient
from .socket_client import AsyncSpeechEngineSocketClient, SpeechEngineSocketClient

try:
    from websockets.legacy.client import connect as websockets_client_connect  # type: ignore
except ImportError:
    from websockets import connect as websockets_client_connect  # type: ignore

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SpeechEngineClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSpeechEngineClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSpeechEngineClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSpeechEngineClient
        """
        return self._raw_client

    def list(
        self,
        *,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        sort_direction: typing.Optional[SortDirection] = None,
        sort_by: typing.Optional[AgentSortBy] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListSpeechEnginesResponse:
        """
        Returns a paginated list of Speech Engine resources.

        Parameters
        ----------
        page_size : typing.Optional[int]
            How many Speech Engines to return at maximum. Can not exceed 100, defaults to 30.

        search : typing.Optional[str]
            Search term to filter Speech Engines by name

        sort_direction : typing.Optional[SortDirection]
            The direction to sort the results

        sort_by : typing.Optional[AgentSortBy]
            The field to sort the results by

        cursor : typing.Optional[str]
            Used for fetching next page. Cursor is returned in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListSpeechEnginesResponse
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.speech_engine.list(
            page_size=1,
            search="search",
            sort_direction="asc",
            sort_by="name",
            cursor="cursor",
        )
        """
        _response = self._raw_client.list(
            page_size=page_size,
            search=search,
            sort_direction=sort_direction,
            sort_by=sort_by,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    def create(
        self,
        *,
        speech_engine: SpeechEngineConfig,
        name: typing.Optional[str] = OMIT,
        asr: typing.Optional[AsrConversationalConfig] = OMIT,
        tts: typing.Optional[TtsConversationalConfigInput] = OMIT,
        turn: typing.Optional[BaseTurnConfig] = OMIT,
        vad: typing.Optional[VadConfig] = OMIT,
        conversation: typing.Optional[ConversationConfigInput] = OMIT,
        privacy: typing.Optional[PrivacyConfigInput] = OMIT,
        call_limits: typing.Optional[AgentCallLimits] = OMIT,
        language: typing.Optional[str] = OMIT,
        tags: typing.Optional[typing.Sequence[str]] = OMIT,
        overrides: typing.Optional[SpeechEngineConversationInitiationClientDataConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SpeechEngineResponse:
        """
        Create a new Speech Engine resource

        Parameters
        ----------
        speech_engine : SpeechEngineConfig
            Speech engine WebSocket configuration

        name : typing.Optional[str]
            Name of the speech engine

        asr : typing.Optional[AsrConversationalConfig]
            ASR configuration

        tts : typing.Optional[TtsConversationalConfigInput]
            TTS configuration

        turn : typing.Optional[BaseTurnConfig]
            Turn detection configuration

        vad : typing.Optional[VadConfig]
            Configuration for voice activity detection

        conversation : typing.Optional[ConversationConfigInput]
            Conversation configuration (client events, etc.)

        privacy : typing.Optional[PrivacyConfigInput]
            Privacy settings (recording, retention, zero retention mode)

        call_limits : typing.Optional[AgentCallLimits]
            Concurrency and daily conversation limits for this speech engine

        language : typing.Optional[str]
            Language for the speech engine

        tags : typing.Optional[typing.Sequence[str]]
            Tags for categorization

        overrides : typing.Optional[SpeechEngineConversationInitiationClientDataConfig]
            Override settings the client may set during conversation initiation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpeechEngineResponse
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs, SpeechEngineConfig

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.speech_engine.create(
            speech_engine=SpeechEngineConfig(
                ws_url="ws_url",
            ),
        )
        """
        _response = self._raw_client.create(
            speech_engine=speech_engine,
            name=name,
            asr=asr,
            tts=tts,
            turn=turn,
            vad=vad,
            conversation=conversation,
            privacy=privacy,
            call_limits=call_limits,
            language=language,
            tags=tags,
            overrides=overrides,
            request_options=request_options,
        )
        return _response.data

    def get(
        self, speech_engine_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SpeechEngineResponse:
        """
        Retrieve a Speech Engine resource

        Parameters
        ----------
        speech_engine_id : str
            The speech engine ID (accepts seng_ or agent_ prefix)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpeechEngineResponse
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.speech_engine.get(
            speech_engine_id="seng_3701k3ttaq12ewp8b7qv5rfyszkz",
        )
        """
        _response = self._raw_client.get(speech_engine_id, request_options=request_options)
        return _response.data

    def delete(self, speech_engine_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete a Speech Engine resource

        Parameters
        ----------
        speech_engine_id : str
            The speech engine ID (accepts seng_ or agent_ prefix)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.speech_engine.delete(
            speech_engine_id="seng_3701k3ttaq12ewp8b7qv5rfyszkz",
        )
        """
        _response = self._raw_client.delete(speech_engine_id, request_options=request_options)
        return _response.data

    def update(
        self,
        speech_engine_id: str,
        *,
        name: typing.Optional[str] = OMIT,
        speech_engine: typing.Optional[SpeechEngineConfig] = OMIT,
        asr: typing.Optional[AsrConversationalConfig] = OMIT,
        tts: typing.Optional[TtsConversationalConfigInput] = OMIT,
        turn: typing.Optional[BaseTurnConfig] = OMIT,
        vad: typing.Optional[VadConfig] = OMIT,
        conversation: typing.Optional[ConversationConfigInput] = OMIT,
        privacy: typing.Optional[PrivacyConfigInput] = OMIT,
        call_limits: typing.Optional[AgentCallLimits] = OMIT,
        language: typing.Optional[str] = OMIT,
        tags: typing.Optional[typing.Sequence[str]] = OMIT,
        overrides: typing.Optional[SpeechEngineConversationInitiationClientDataConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SpeechEngineResponse:
        """
        Update a Speech Engine resource (partial update)

        Parameters
        ----------
        speech_engine_id : str
            The speech engine ID (accepts seng_ or agent_ prefix)

        name : typing.Optional[str]

        speech_engine : typing.Optional[SpeechEngineConfig]

        asr : typing.Optional[AsrConversationalConfig]

        tts : typing.Optional[TtsConversationalConfigInput]

        turn : typing.Optional[BaseTurnConfig]

        vad : typing.Optional[VadConfig]

        conversation : typing.Optional[ConversationConfigInput]

        privacy : typing.Optional[PrivacyConfigInput]

        call_limits : typing.Optional[AgentCallLimits]

        language : typing.Optional[str]

        tags : typing.Optional[typing.Sequence[str]]

        overrides : typing.Optional[SpeechEngineConversationInitiationClientDataConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpeechEngineResponse
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.speech_engine.update(
            speech_engine_id="seng_3701k3ttaq12ewp8b7qv5rfyszkz",
        )
        """
        _response = self._raw_client.update(
            speech_engine_id,
            name=name,
            speech_engine=speech_engine,
            asr=asr,
            tts=tts,
            turn=turn,
            vad=vad,
            conversation=conversation,
            privacy=privacy,
            call_limits=call_limits,
            language=language,
            tags=tags,
            overrides=overrides,
            request_options=request_options,
        )
        return _response.data

    @contextmanager
    def upstream(
        self,
        *,
        eleven_labs_speech_engine_authorization: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[SpeechEngineSocketClient]:
        """
        The Speech Engine upstream WebSocket protocol defines the interface your server must
        implement so that ElevenLabs can connect to it during a Speech Engine conversation.
        Unlike other ElevenLabs WebSocket channels where your client connects to ElevenLabs,
        the Speech Engine reverses this relationship: **ElevenLabs is the WebSocket client and
        your server is the WebSocket server**.

        This page shows the WebSocket API shape, however we recommend using the provided server side SDKs
        instead of implementing this yourself. The SDKs include several helper methods and automatically
        handle auth for you. You can find SDK installation instructions and
        guides in the [Speech Engine quickstart](/docs/eleven-api/guides/cookbooks/speech-engine).

        Configure your server's publicly reachable WebSocket URL in the `wsUrl` field when
        creating or updating a Speech Engine via the REST API. When a user starts a conversation
        with that agent, ElevenLabs will open a WebSocket connection to your server and begin
        the message exchange described below.

        ## Connection flow

        1. A user starts a conversation with a Speech Engine agent (via the ElevenLabs client SDK or API).
        2. ElevenLabs opens a WebSocket connection to your `wsUrl`.
        3. ElevenLabs sends an `init` message containing the conversation ID.
        4. As the user speaks, ElevenLabs transcribes the audio and sends `user_transcript` messages with the full conversation history.
        5. Your server calls an LLM and streams the response back as one or more `agent_response` messages.
        6. ElevenLabs synthesizes the text to speech and streams the audio back to the user.
        7. Periodic `ping` messages keep the connection alive; reply with `pong`.
        8. When the conversation ends, ElevenLabs sends a `close` message.

        ## Authentication

        Every connection from ElevenLabs includes an `X-Elevenlabs-Speech-Engine-Authorization`
        header containing a short-lived JWT. Verify this token before accepting the WebSocket
        upgrade to ensure the connection originates from ElevenLabs.

        The JWT is signed with **HS256** using the SHA-256 hash of your ElevenLabs API key as
        the HMAC secret, and has:
        - **Issuer** (`iss`): `https://api.elevenlabs.io/convai/speech-engine`
        - **Subject** (`sub`): `convai_speech_engine_upstream`
        - **Expiry** (`exp`): short-lived; a 60-second clock-skew leeway is applied

        ## Interruption handling

        Each `user_transcript` message carries an `event_id`. If the user speaks again before
        your server finishes responding, a new `user_transcript` arrives with a higher `event_id`.
        Cancel your in-flight LLM call and begin responding to the new transcript. Any
        `agent_response` messages sent with an outdated `event_id` are silently discarded by
        ElevenLabs.

        ## Streaming responses

        Send LLM output as a sequence of `agent_response` messages with `is_final: false` for
        each text chunk, followed by a final `agent_response` with `is_final: true` and an empty
        `content` string. ElevenLabs begins synthesizing audio as chunks arrive, minimising
        latency.

        Parameters
        ----------
        eleven_labs_speech_engine_authorization : typing.Optional[str]
            Short-lived JWT proving the connection originates from ElevenLabs. Signed with HS256 using the SHA-256 hash of your ElevenLabs API key. Validate the issuer (https://api.elevenlabs.io/convai/speech-engine) and subject (convai_speech_engine_upstream) before accepting the upgrade.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpeechEngineSocketClient
        """
        ws_url = (
            # Manual fix for a fern-python-sdk bug: websockets rejects the
            # http(s) scheme of the base URL. Carried forward by fern-replay
            # until the generator is fixed upstream.
            self._raw_client._client_wrapper.get_base_url().replace("http://", "ws://", 1).replace("https://", "wss://", 1)
            + "/speech-engine/upstream"
        )
        _encoded_query_params = encode_query(
            jsonable_encoder(
                remove_none_from_dict(
                    {
                        **(
                            request_options.get("additional_query_parameters", {}) or {}
                            if request_options is not None
                            else {}
                        ),
                    }
                )
            )
        )
        if _encoded_query_params:
            ws_url = ws_url + "?" + urllib.parse.urlencode(_encoded_query_params)
        headers = self._raw_client._client_wrapper.get_headers()
        if eleven_labs_speech_engine_authorization is not None:
            headers["X-ElevenLabs-Speech-Engine-Authorization"] = str(eleven_labs_speech_engine_authorization)
        if request_options and "additional_headers" in request_options:
            headers.update(request_options["additional_headers"])
        try:
            with websockets_sync_client.connect(ws_url, additional_headers=headers) as protocol:
                yield SpeechEngineSocketClient(websocket=protocol)
        except InvalidWebSocketStatus as exc:
            status_code: int = get_status_code(exc)
            if status_code == 401:
                raise ApiError(
                    status_code=status_code,
                    headers=dict(headers),
                    body="Websocket initialized with invalid credentials.",
                )
            raise ApiError(
                status_code=status_code,
                headers=dict(headers),
                body="Unexpected error when initializing websocket connection.",
            )


class AsyncSpeechEngineClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSpeechEngineClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSpeechEngineClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSpeechEngineClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        sort_direction: typing.Optional[SortDirection] = None,
        sort_by: typing.Optional[AgentSortBy] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListSpeechEnginesResponse:
        """
        Returns a paginated list of Speech Engine resources.

        Parameters
        ----------
        page_size : typing.Optional[int]
            How many Speech Engines to return at maximum. Can not exceed 100, defaults to 30.

        search : typing.Optional[str]
            Search term to filter Speech Engines by name

        sort_direction : typing.Optional[SortDirection]
            The direction to sort the results

        sort_by : typing.Optional[AgentSortBy]
            The field to sort the results by

        cursor : typing.Optional[str]
            Used for fetching next page. Cursor is returned in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListSpeechEnginesResponse
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.speech_engine.list(
                page_size=1,
                search="search",
                sort_direction="asc",
                sort_by="name",
                cursor="cursor",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            page_size=page_size,
            search=search,
            sort_direction=sort_direction,
            sort_by=sort_by,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    async def create(
        self,
        *,
        speech_engine: SpeechEngineConfig,
        name: typing.Optional[str] = OMIT,
        asr: typing.Optional[AsrConversationalConfig] = OMIT,
        tts: typing.Optional[TtsConversationalConfigInput] = OMIT,
        turn: typing.Optional[BaseTurnConfig] = OMIT,
        vad: typing.Optional[VadConfig] = OMIT,
        conversation: typing.Optional[ConversationConfigInput] = OMIT,
        privacy: typing.Optional[PrivacyConfigInput] = OMIT,
        call_limits: typing.Optional[AgentCallLimits] = OMIT,
        language: typing.Optional[str] = OMIT,
        tags: typing.Optional[typing.Sequence[str]] = OMIT,
        overrides: typing.Optional[SpeechEngineConversationInitiationClientDataConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SpeechEngineResponse:
        """
        Create a new Speech Engine resource

        Parameters
        ----------
        speech_engine : SpeechEngineConfig
            Speech engine WebSocket configuration

        name : typing.Optional[str]
            Name of the speech engine

        asr : typing.Optional[AsrConversationalConfig]
            ASR configuration

        tts : typing.Optional[TtsConversationalConfigInput]
            TTS configuration

        turn : typing.Optional[BaseTurnConfig]
            Turn detection configuration

        vad : typing.Optional[VadConfig]
            Configuration for voice activity detection

        conversation : typing.Optional[ConversationConfigInput]
            Conversation configuration (client events, etc.)

        privacy : typing.Optional[PrivacyConfigInput]
            Privacy settings (recording, retention, zero retention mode)

        call_limits : typing.Optional[AgentCallLimits]
            Concurrency and daily conversation limits for this speech engine

        language : typing.Optional[str]
            Language for the speech engine

        tags : typing.Optional[typing.Sequence[str]]
            Tags for categorization

        overrides : typing.Optional[SpeechEngineConversationInitiationClientDataConfig]
            Override settings the client may set during conversation initiation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpeechEngineResponse
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs, SpeechEngineConfig

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.speech_engine.create(
                speech_engine=SpeechEngineConfig(
                    ws_url="ws_url",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            speech_engine=speech_engine,
            name=name,
            asr=asr,
            tts=tts,
            turn=turn,
            vad=vad,
            conversation=conversation,
            privacy=privacy,
            call_limits=call_limits,
            language=language,
            tags=tags,
            overrides=overrides,
            request_options=request_options,
        )
        return _response.data

    async def get(
        self, speech_engine_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SpeechEngineResponse:
        """
        Retrieve a Speech Engine resource

        Parameters
        ----------
        speech_engine_id : str
            The speech engine ID (accepts seng_ or agent_ prefix)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpeechEngineResponse
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.speech_engine.get(
                speech_engine_id="seng_3701k3ttaq12ewp8b7qv5rfyszkz",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(speech_engine_id, request_options=request_options)
        return _response.data

    async def delete(self, speech_engine_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete a Speech Engine resource

        Parameters
        ----------
        speech_engine_id : str
            The speech engine ID (accepts seng_ or agent_ prefix)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.speech_engine.delete(
                speech_engine_id="seng_3701k3ttaq12ewp8b7qv5rfyszkz",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(speech_engine_id, request_options=request_options)
        return _response.data

    async def update(
        self,
        speech_engine_id: str,
        *,
        name: typing.Optional[str] = OMIT,
        speech_engine: typing.Optional[SpeechEngineConfig] = OMIT,
        asr: typing.Optional[AsrConversationalConfig] = OMIT,
        tts: typing.Optional[TtsConversationalConfigInput] = OMIT,
        turn: typing.Optional[BaseTurnConfig] = OMIT,
        vad: typing.Optional[VadConfig] = OMIT,
        conversation: typing.Optional[ConversationConfigInput] = OMIT,
        privacy: typing.Optional[PrivacyConfigInput] = OMIT,
        call_limits: typing.Optional[AgentCallLimits] = OMIT,
        language: typing.Optional[str] = OMIT,
        tags: typing.Optional[typing.Sequence[str]] = OMIT,
        overrides: typing.Optional[SpeechEngineConversationInitiationClientDataConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SpeechEngineResponse:
        """
        Update a Speech Engine resource (partial update)

        Parameters
        ----------
        speech_engine_id : str
            The speech engine ID (accepts seng_ or agent_ prefix)

        name : typing.Optional[str]

        speech_engine : typing.Optional[SpeechEngineConfig]

        asr : typing.Optional[AsrConversationalConfig]

        tts : typing.Optional[TtsConversationalConfigInput]

        turn : typing.Optional[BaseTurnConfig]

        vad : typing.Optional[VadConfig]

        conversation : typing.Optional[ConversationConfigInput]

        privacy : typing.Optional[PrivacyConfigInput]

        call_limits : typing.Optional[AgentCallLimits]

        language : typing.Optional[str]

        tags : typing.Optional[typing.Sequence[str]]

        overrides : typing.Optional[SpeechEngineConversationInitiationClientDataConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpeechEngineResponse
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.speech_engine.update(
                speech_engine_id="seng_3701k3ttaq12ewp8b7qv5rfyszkz",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            speech_engine_id,
            name=name,
            speech_engine=speech_engine,
            asr=asr,
            tts=tts,
            turn=turn,
            vad=vad,
            conversation=conversation,
            privacy=privacy,
            call_limits=call_limits,
            language=language,
            tags=tags,
            overrides=overrides,
            request_options=request_options,
        )
        return _response.data

    @asynccontextmanager
    async def upstream(
        self,
        *,
        eleven_labs_speech_engine_authorization: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[AsyncSpeechEngineSocketClient]:
        """
        The Speech Engine upstream WebSocket protocol defines the interface your server must
        implement so that ElevenLabs can connect to it during a Speech Engine conversation.
        Unlike other ElevenLabs WebSocket channels where your client connects to ElevenLabs,
        the Speech Engine reverses this relationship: **ElevenLabs is the WebSocket client and
        your server is the WebSocket server**.

        This page shows the WebSocket API shape, however we recommend using the provided server side SDKs
        instead of implementing this yourself. The SDKs include several helper methods and automatically
        handle auth for you. You can find SDK installation instructions and
        guides in the [Speech Engine quickstart](/docs/eleven-api/guides/cookbooks/speech-engine).

        Configure your server's publicly reachable WebSocket URL in the `wsUrl` field when
        creating or updating a Speech Engine via the REST API. When a user starts a conversation
        with that agent, ElevenLabs will open a WebSocket connection to your server and begin
        the message exchange described below.

        ## Connection flow

        1. A user starts a conversation with a Speech Engine agent (via the ElevenLabs client SDK or API).
        2. ElevenLabs opens a WebSocket connection to your `wsUrl`.
        3. ElevenLabs sends an `init` message containing the conversation ID.
        4. As the user speaks, ElevenLabs transcribes the audio and sends `user_transcript` messages with the full conversation history.
        5. Your server calls an LLM and streams the response back as one or more `agent_response` messages.
        6. ElevenLabs synthesizes the text to speech and streams the audio back to the user.
        7. Periodic `ping` messages keep the connection alive; reply with `pong`.
        8. When the conversation ends, ElevenLabs sends a `close` message.

        ## Authentication

        Every connection from ElevenLabs includes an `X-Elevenlabs-Speech-Engine-Authorization`
        header containing a short-lived JWT. Verify this token before accepting the WebSocket
        upgrade to ensure the connection originates from ElevenLabs.

        The JWT is signed with **HS256** using the SHA-256 hash of your ElevenLabs API key as
        the HMAC secret, and has:
        - **Issuer** (`iss`): `https://api.elevenlabs.io/convai/speech-engine`
        - **Subject** (`sub`): `convai_speech_engine_upstream`
        - **Expiry** (`exp`): short-lived; a 60-second clock-skew leeway is applied

        ## Interruption handling

        Each `user_transcript` message carries an `event_id`. If the user speaks again before
        your server finishes responding, a new `user_transcript` arrives with a higher `event_id`.
        Cancel your in-flight LLM call and begin responding to the new transcript. Any
        `agent_response` messages sent with an outdated `event_id` are silently discarded by
        ElevenLabs.

        ## Streaming responses

        Send LLM output as a sequence of `agent_response` messages with `is_final: false` for
        each text chunk, followed by a final `agent_response` with `is_final: true` and an empty
        `content` string. ElevenLabs begins synthesizing audio as chunks arrive, minimising
        latency.

        Parameters
        ----------
        eleven_labs_speech_engine_authorization : typing.Optional[str]
            Short-lived JWT proving the connection originates from ElevenLabs. Signed with HS256 using the SHA-256 hash of your ElevenLabs API key. Validate the issuer (https://api.elevenlabs.io/convai/speech-engine) and subject (convai_speech_engine_upstream) before accepting the upgrade.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncSpeechEngineSocketClient
        """
        ws_url = (
            # Manual fix for a fern-python-sdk bug: websockets rejects the
            # http(s) scheme of the base URL. Carried forward by fern-replay
            # until the generator is fixed upstream.
            self._raw_client._client_wrapper.get_base_url().replace("http://", "ws://", 1).replace("https://", "wss://", 1)
            + "/speech-engine/upstream"
        )
        _encoded_query_params = encode_query(
            jsonable_encoder(
                remove_none_from_dict(
                    {
                        **(
                            request_options.get("additional_query_parameters", {}) or {}
                            if request_options is not None
                            else {}
                        ),
                    }
                )
            )
        )
        if _encoded_query_params:
            ws_url = ws_url + "?" + urllib.parse.urlencode(_encoded_query_params)
        headers = self._raw_client._client_wrapper.get_headers()
        if eleven_labs_speech_engine_authorization is not None:
            headers["X-ElevenLabs-Speech-Engine-Authorization"] = str(eleven_labs_speech_engine_authorization)
        if request_options and "additional_headers" in request_options:
            headers.update(request_options["additional_headers"])
        try:
            async with websockets_client_connect(ws_url, extra_headers=headers) as protocol:
                yield AsyncSpeechEngineSocketClient(websocket=protocol)
        except InvalidWebSocketStatus as exc:
            status_code: int = get_status_code(exc)
            if status_code == 401:
                raise ApiError(
                    status_code=status_code,
                    headers=dict(headers),
                    body="Websocket initialized with invalid credentials.",
                )
            raise ApiError(
                status_code=status_code,
                headers=dict(headers),
                body="Unexpected error when initializing websocket connection.",
            )
