# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ...types.agent_conversation_ticket_issue_type import AgentConversationTicketIssueType
from ...types.agent_conversation_ticket_response_model import AgentConversationTicketResponseModel
from ...types.agent_conversation_ticket_source import AgentConversationTicketSource
from ...types.agent_conversation_ticket_status import AgentConversationTicketStatus
from ...types.assignable_user_response_model import AssignableUserResponseModel
from ...types.get_agent_conversation_tickets_page_response_model import GetAgentConversationTicketsPageResponseModel
from ...types.turn_comment_request_model import TurnCommentRequestModel
from .raw_client import AsyncRawTriageTicketsClient, RawTriageTicketsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class TriageTicketsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTriageTicketsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTriageTicketsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTriageTicketsClient
        """
        return self._raw_client

    def list(
        self,
        agent_id: str,
        *,
        page_size: typing.Optional[int] = None,
        conversation_id: typing.Optional[str] = None,
        status: typing.Optional[AgentConversationTicketStatus] = None,
        sources: typing.Optional[
            typing.Union[AgentConversationTicketSource, typing.Sequence[AgentConversationTicketSource]]
        ] = None,
        owner_user_id: typing.Optional[str] = None,
        assignee_user_id: typing.Optional[str] = None,
        issue_type: typing.Optional[AgentConversationTicketIssueType] = None,
        label: typing.Optional[str] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentConversationTicketsPageResponseModel:
        """
        List an agent's conversation triage tickets, ordered by most recently created first. These are tickets about the agent's own performance on a conversation (for triage with Architect), not tickets an agent opens for end users.

        Parameters
        ----------
        agent_id : str

        page_size : typing.Optional[int]
            How many agent conversation tickets to return. Can not exceed 100.

        conversation_id : typing.Optional[str]
            Filter tickets by conversation id.

        status : typing.Optional[AgentConversationTicketStatus]
            Filter tickets by status.

        sources : typing.Optional[typing.Union[AgentConversationTicketSource, typing.Sequence[AgentConversationTicketSource]]]
            Filter tickets by how they were raised (qa, agent, manual). Repeat the parameter to filter by multiple sources.

        owner_user_id : typing.Optional[str]
            Filter tickets by creator. Use 'agent' for agent-raised tickets.

        assignee_user_id : typing.Optional[str]
            Filter tickets by assignee. Use 'unassigned' for tickets with no assignee.

        issue_type : typing.Optional[AgentConversationTicketIssueType]
            Filter clusters by issue type.

        label : typing.Optional[str]
            Filter tickets by an exact label.

        cursor : typing.Optional[str]
            Used for fetching next page. Cursor is returned in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentConversationTicketsPageResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.list(
            agent_id="agent_id",
            page_size=1,
            conversation_id="conversation_id",
            status="open",
            sources=["qa"],
            owner_user_id="owner_user_id",
            assignee_user_id="assignee_user_id",
            issue_type="knowledge_gap",
            label="label",
            cursor="cursor",
        )
        """
        _response = self._raw_client.list(
            agent_id,
            page_size=page_size,
            conversation_id=conversation_id,
            status=status,
            sources=sources,
            owner_user_id=owner_user_id,
            assignee_user_id=assignee_user_id,
            issue_type=issue_type,
            label=label,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    def create_manual(
        self, agent_id: str, *, qa_comment: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AgentConversationTicketResponseModel:
        """
        Manually raise a follow-up ticket against an agent, not tied to any conversation (for example a task like 'add the KB about X'). The comment is shown as the ticket title. Requires viewer access to the agent.

        Parameters
        ----------
        agent_id : str

        qa_comment : str
            What the ticket is about, e.g. a follow-up task for the agent. This is shown as the ticket title.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.create_manual(
            agent_id="agent_id",
            qa_comment="qa_comment",
        )
        """
        _response = self._raw_client.create_manual(agent_id, qa_comment=qa_comment, request_options=request_options)
        return _response.data

    def list_for_workspace(
        self,
        *,
        page_size: typing.Optional[int] = None,
        status: typing.Optional[AgentConversationTicketStatus] = None,
        assignee_user_id: typing.Optional[str] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentConversationTicketsPageResponseModel:
        """
        List conversation triage tickets across every agent in the workspace, ordered by most recently created first. Use this to build a workspace-wide view (for example, tickets assigned to the caller); for a single agent's tickets, use the per-agent endpoint instead. Tickets for agents the caller cannot access are omitted.

        Parameters
        ----------
        page_size : typing.Optional[int]
            How many agent conversation tickets to return. Can not exceed 100.

        status : typing.Optional[AgentConversationTicketStatus]
            Filter tickets by status.

        assignee_user_id : typing.Optional[str]
            Filter tickets by assignee. Use 'unassigned' for tickets with no assignee.

        cursor : typing.Optional[str]
            Used for fetching next page. Cursor is returned in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentConversationTicketsPageResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.list_for_workspace(
            page_size=1,
            status="open",
            assignee_user_id="assignee_user_id",
            cursor="cursor",
        )
        """
        _response = self._raw_client.list_for_workspace(
            page_size=page_size,
            status=status,
            assignee_user_id=assignee_user_id,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    def create(
        self,
        *,
        conversation_id: str,
        qa_comment: typing.Optional[str] = OMIT,
        turn_comments: typing.Optional[typing.Sequence[TurnCommentRequestModel]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentConversationTicketResponseModel:
        """
        Raise a ticket about an agent's performance on a conversation, for triage with Architect. Provide an overall comment and/or turn-level comments describing what went wrong.

        Parameters
        ----------
        conversation_id : str
            Conversation this ticket is about.

        qa_comment : typing.Optional[str]
            The QA finding covering the whole conversation.

        turn_comments : typing.Optional[typing.Sequence[TurnCommentRequestModel]]
            Optional turn-level comments on what went wrong.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.create(
            conversation_id="conversation_id",
        )
        """
        _response = self._raw_client.create(
            conversation_id=conversation_id,
            qa_comment=qa_comment,
            turn_comments=turn_comments,
            request_options=request_options,
        )
        return _response.data

    def list_assignable_users(
        self, agent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[AssignableUserResponseModel]:
        """
        All non-service-account workspace members, each flagged with whether they currently have at least viewer access to the agent. Members without access are included (not filtered out) so the UI can offer them as an assignee and prompt to grant access first.

        Parameters
        ----------
        agent_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[AssignableUserResponseModel]
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.list_assignable_users(
            agent_id="agent_id",
        )
        """
        _response = self._raw_client.list_assignable_users(agent_id, request_options=request_options)
        return _response.data

    def get(
        self, agentqa_ticket_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AgentConversationTicketResponseModel:
        """
        Get an agent conversation ticket by ID.

        Parameters
        ----------
        agentqa_ticket_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.get(
            agentqa_ticket_id="agentqa_ticket_id",
        )
        """
        _response = self._raw_client.get(agentqa_ticket_id, request_options=request_options)
        return _response.data

    def delete(self, agentqa_ticket_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete an agent conversation ticket. Restricted to the ticket creator or a workspace admin.

        Parameters
        ----------
        agentqa_ticket_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.delete(
            agentqa_ticket_id="agentqa_ticket_id",
        )
        """
        _response = self._raw_client.delete(agentqa_ticket_id, request_options=request_options)
        return _response.data

    def update(
        self,
        agentqa_ticket_id: str,
        *,
        status: typing.Optional[AgentConversationTicketStatus] = OMIT,
        assignee_user_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentConversationTicketResponseModel:
        """
        Update a ticket's comment, status, and/or assignee. Requires editor access to the ticket's agent.

        Parameters
        ----------
        agentqa_ticket_id : str

        status : typing.Optional[AgentConversationTicketStatus]
            If provided, updates the ticket status. Omit to leave unchanged.

        assignee_user_id : typing.Optional[str]
            If provided, updates who is responsible for resolving this ticket. Must be a workspace member with at least viewer access to the agent. Pass null to unassign. Omit to leave unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.update(
            agentqa_ticket_id="agentqa_ticket_id",
        )
        """
        _response = self._raw_client.update(
            agentqa_ticket_id, status=status, assignee_user_id=assignee_user_id, request_options=request_options
        )
        return _response.data

    def add_comment(
        self, agentqa_ticket_id: str, *, comment: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AgentConversationTicketResponseModel:
        """
        Append a comment discussing how to resolve the ticket. Requires viewer access to the ticket's agent.

        Parameters
        ----------
        agentqa_ticket_id : str

        comment : str
            A comment discussing how to resolve the ticket.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.add_comment(
            agentqa_ticket_id="agentqa_ticket_id",
            comment="comment",
        )
        """
        _response = self._raw_client.add_comment(agentqa_ticket_id, comment=comment, request_options=request_options)
        return _response.data

    def add_turn_comment(
        self,
        agentqa_ticket_id: str,
        *,
        turn_index: int,
        comment: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentConversationTicketResponseModel:
        """
        Append a turn-level comment to a ticket. Requires viewer access to the ticket's agent.

        Parameters
        ----------
        agentqa_ticket_id : str

        turn_index : int
            Zero-based index of the transcript turn this comment refers to.

        comment : str
            What went wrong at this turn.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        from elevenlabs import ElevenLabs

        client = ElevenLabs(
            api_key="YOUR_API_KEY",
        )
        client.agents.triage_tickets.add_turn_comment(
            agentqa_ticket_id="agentqa_ticket_id",
            turn_index=1,
            comment="comment",
        )
        """
        _response = self._raw_client.add_turn_comment(
            agentqa_ticket_id, turn_index=turn_index, comment=comment, request_options=request_options
        )
        return _response.data


class AsyncTriageTicketsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTriageTicketsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTriageTicketsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTriageTicketsClient
        """
        return self._raw_client

    async def list(
        self,
        agent_id: str,
        *,
        page_size: typing.Optional[int] = None,
        conversation_id: typing.Optional[str] = None,
        status: typing.Optional[AgentConversationTicketStatus] = None,
        sources: typing.Optional[
            typing.Union[AgentConversationTicketSource, typing.Sequence[AgentConversationTicketSource]]
        ] = None,
        owner_user_id: typing.Optional[str] = None,
        assignee_user_id: typing.Optional[str] = None,
        issue_type: typing.Optional[AgentConversationTicketIssueType] = None,
        label: typing.Optional[str] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentConversationTicketsPageResponseModel:
        """
        List an agent's conversation triage tickets, ordered by most recently created first. These are tickets about the agent's own performance on a conversation (for triage with Architect), not tickets an agent opens for end users.

        Parameters
        ----------
        agent_id : str

        page_size : typing.Optional[int]
            How many agent conversation tickets to return. Can not exceed 100.

        conversation_id : typing.Optional[str]
            Filter tickets by conversation id.

        status : typing.Optional[AgentConversationTicketStatus]
            Filter tickets by status.

        sources : typing.Optional[typing.Union[AgentConversationTicketSource, typing.Sequence[AgentConversationTicketSource]]]
            Filter tickets by how they were raised (qa, agent, manual). Repeat the parameter to filter by multiple sources.

        owner_user_id : typing.Optional[str]
            Filter tickets by creator. Use 'agent' for agent-raised tickets.

        assignee_user_id : typing.Optional[str]
            Filter tickets by assignee. Use 'unassigned' for tickets with no assignee.

        issue_type : typing.Optional[AgentConversationTicketIssueType]
            Filter clusters by issue type.

        label : typing.Optional[str]
            Filter tickets by an exact label.

        cursor : typing.Optional[str]
            Used for fetching next page. Cursor is returned in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentConversationTicketsPageResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.list(
                agent_id="agent_id",
                page_size=1,
                conversation_id="conversation_id",
                status="open",
                sources=["qa"],
                owner_user_id="owner_user_id",
                assignee_user_id="assignee_user_id",
                issue_type="knowledge_gap",
                label="label",
                cursor="cursor",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            agent_id,
            page_size=page_size,
            conversation_id=conversation_id,
            status=status,
            sources=sources,
            owner_user_id=owner_user_id,
            assignee_user_id=assignee_user_id,
            issue_type=issue_type,
            label=label,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    async def create_manual(
        self, agent_id: str, *, qa_comment: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AgentConversationTicketResponseModel:
        """
        Manually raise a follow-up ticket against an agent, not tied to any conversation (for example a task like 'add the KB about X'). The comment is shown as the ticket title. Requires viewer access to the agent.

        Parameters
        ----------
        agent_id : str

        qa_comment : str
            What the ticket is about, e.g. a follow-up task for the agent. This is shown as the ticket title.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.create_manual(
                agent_id="agent_id",
                qa_comment="qa_comment",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_manual(
            agent_id, qa_comment=qa_comment, request_options=request_options
        )
        return _response.data

    async def list_for_workspace(
        self,
        *,
        page_size: typing.Optional[int] = None,
        status: typing.Optional[AgentConversationTicketStatus] = None,
        assignee_user_id: typing.Optional[str] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentConversationTicketsPageResponseModel:
        """
        List conversation triage tickets across every agent in the workspace, ordered by most recently created first. Use this to build a workspace-wide view (for example, tickets assigned to the caller); for a single agent's tickets, use the per-agent endpoint instead. Tickets for agents the caller cannot access are omitted.

        Parameters
        ----------
        page_size : typing.Optional[int]
            How many agent conversation tickets to return. Can not exceed 100.

        status : typing.Optional[AgentConversationTicketStatus]
            Filter tickets by status.

        assignee_user_id : typing.Optional[str]
            Filter tickets by assignee. Use 'unassigned' for tickets with no assignee.

        cursor : typing.Optional[str]
            Used for fetching next page. Cursor is returned in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentConversationTicketsPageResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.list_for_workspace(
                page_size=1,
                status="open",
                assignee_user_id="assignee_user_id",
                cursor="cursor",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_for_workspace(
            page_size=page_size,
            status=status,
            assignee_user_id=assignee_user_id,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    async def create(
        self,
        *,
        conversation_id: str,
        qa_comment: typing.Optional[str] = OMIT,
        turn_comments: typing.Optional[typing.Sequence[TurnCommentRequestModel]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentConversationTicketResponseModel:
        """
        Raise a ticket about an agent's performance on a conversation, for triage with Architect. Provide an overall comment and/or turn-level comments describing what went wrong.

        Parameters
        ----------
        conversation_id : str
            Conversation this ticket is about.

        qa_comment : typing.Optional[str]
            The QA finding covering the whole conversation.

        turn_comments : typing.Optional[typing.Sequence[TurnCommentRequestModel]]
            Optional turn-level comments on what went wrong.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.create(
                conversation_id="conversation_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            conversation_id=conversation_id,
            qa_comment=qa_comment,
            turn_comments=turn_comments,
            request_options=request_options,
        )
        return _response.data

    async def list_assignable_users(
        self, agent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[AssignableUserResponseModel]:
        """
        All non-service-account workspace members, each flagged with whether they currently have at least viewer access to the agent. Members without access are included (not filtered out) so the UI can offer them as an assignee and prompt to grant access first.

        Parameters
        ----------
        agent_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[AssignableUserResponseModel]
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.list_assignable_users(
                agent_id="agent_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_assignable_users(agent_id, request_options=request_options)
        return _response.data

    async def get(
        self, agentqa_ticket_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AgentConversationTicketResponseModel:
        """
        Get an agent conversation ticket by ID.

        Parameters
        ----------
        agentqa_ticket_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.get(
                agentqa_ticket_id="agentqa_ticket_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(agentqa_ticket_id, request_options=request_options)
        return _response.data

    async def delete(self, agentqa_ticket_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete an agent conversation ticket. Restricted to the ticket creator or a workspace admin.

        Parameters
        ----------
        agentqa_ticket_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.delete(
                agentqa_ticket_id="agentqa_ticket_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(agentqa_ticket_id, request_options=request_options)
        return _response.data

    async def update(
        self,
        agentqa_ticket_id: str,
        *,
        status: typing.Optional[AgentConversationTicketStatus] = OMIT,
        assignee_user_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentConversationTicketResponseModel:
        """
        Update a ticket's comment, status, and/or assignee. Requires editor access to the ticket's agent.

        Parameters
        ----------
        agentqa_ticket_id : str

        status : typing.Optional[AgentConversationTicketStatus]
            If provided, updates the ticket status. Omit to leave unchanged.

        assignee_user_id : typing.Optional[str]
            If provided, updates who is responsible for resolving this ticket. Must be a workspace member with at least viewer access to the agent. Pass null to unassign. Omit to leave unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.update(
                agentqa_ticket_id="agentqa_ticket_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            agentqa_ticket_id, status=status, assignee_user_id=assignee_user_id, request_options=request_options
        )
        return _response.data

    async def add_comment(
        self, agentqa_ticket_id: str, *, comment: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AgentConversationTicketResponseModel:
        """
        Append a comment discussing how to resolve the ticket. Requires viewer access to the ticket's agent.

        Parameters
        ----------
        agentqa_ticket_id : str

        comment : str
            A comment discussing how to resolve the ticket.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.add_comment(
                agentqa_ticket_id="agentqa_ticket_id",
                comment="comment",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.add_comment(
            agentqa_ticket_id, comment=comment, request_options=request_options
        )
        return _response.data

    async def add_turn_comment(
        self,
        agentqa_ticket_id: str,
        *,
        turn_index: int,
        comment: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentConversationTicketResponseModel:
        """
        Append a turn-level comment to a ticket. Requires viewer access to the ticket's agent.

        Parameters
        ----------
        agentqa_ticket_id : str

        turn_index : int
            Zero-based index of the transcript turn this comment refers to.

        comment : str
            What went wrong at this turn.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentConversationTicketResponseModel
            Successful Response

        Examples
        --------
        import asyncio

        from elevenlabs import AsyncElevenLabs

        client = AsyncElevenLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.triage_tickets.add_turn_comment(
                agentqa_ticket_id="agentqa_ticket_id",
                turn_index=1,
                comment="comment",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.add_turn_comment(
            agentqa_ticket_id, turn_index=turn_index, comment=comment, request_options=request_options
        )
        return _response.data
