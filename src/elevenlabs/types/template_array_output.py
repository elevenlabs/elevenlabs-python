# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, update_forward_refs
from ..core.unchecked_base_model import UncheckedBaseModel
from .template_array_output_failure_reason import TemplateArrayOutputFailureReason
from .template_run_status import TemplateRunStatus


class TemplateArrayOutput(UncheckedBaseModel):
    """
    An `array` output, delivered as `content` once completed: one output per
    element, each an output in its own right with its own `status`.
    """

    id: str = pydantic.Field()
    """
    The id of the generation behind this output. Pass it as a `generation` reference to use the output as an input elsewhere.
    """

    status: TemplateRunStatus = pydantic.Field()
    """
    The lifecycle status of the output. It ends at `completed`, when the output's content fields are set, or `failed`, when `failure_reason` and `error_message` are set.
    """

    failure_reason: typing.Optional[TemplateArrayOutputFailureReason] = pydantic.Field(default=None)
    """
    The category of failure. Present only when `status` is `failed`.
    """

    error_message: typing.Optional[str] = pydantic.Field(default=None)
    """
    A human-readable description of the failure. Present only when `status` is `failed`. Failed generations are not charged.
    """

    content: typing.Optional[typing.List["TemplateOutput"]] = pydantic.Field(default=None)
    """
    One output per element, in order, each following the shape of the schema's `items`. Present only when `status` is `completed`. Reserved: no template produces this kind of output yet; it is published so that templates which do can be run with the same client.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


from .template_object_output import TemplateObjectOutput  # noqa: E402, I001
from .template_output import TemplateOutput  # noqa: E402, I001

update_forward_refs(TemplateArrayOutput, TemplateObjectOutput=TemplateObjectOutput, TemplateOutput=TemplateOutput)
