# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from ..core.unchecked_base_model import UncheckedBaseModel


class FinalAudioForTurnMulti(UncheckedBaseModel):
    is_final_audio_for_turn: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Signals the end of audio for a turn. Emitted after each explicit flush, new_turn/voice-switch boundary, and on connection close. For streaming codecs (MP3, Opus, etc.), the persistent encoder buffers a small amount of audio across turn boundaries, so a few bytes belonging to the marked turn may arrive interleaved with the next turn's first audio chunk. Clients that need exact per-turn boundaries should request a PCM output format, which has no buffering.
    """

    context_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Identifies which context this response belongs to. Only set on the multi-context endpoint; omitted (None) on the single-context endpoint.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
